// Entorno de DESARROLLO — Angular reemplaza este archivo con environment.prod.ts al compilar con --configuration production
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api'
};
